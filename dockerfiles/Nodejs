ARG NODEJS_VERSION
FROM node:${NODEJS_VERSION}

ARG MAINTAINER
ARG NODEJS_EXPOSE

#维护者信息
MAINTAINER ${MAINTAINER}

EXPOSE ${NODEJS_EXPOSE}

# add cnpm for taobao registry
RUN npm install -g cnpm --registry=https://registry.npm.taobao.org

CMD ["npm","start"]